services:
  nginx:
    ports:
      - "${NGINX_PORT}:80"
    networks:
      default: {}
      proxy_manager:
        aliases:
          - system-church-prod-web

networks:
  proxy_manager:
    external: true
